# 提示 2

`ComposableNode` 的 `plugin` 参数是在 C++ 中用
`RCLCPP_COMPONENTS_REGISTER_NODE(ClassName)` 注册的类名。

在本练习中：
- `plugin='PublisherComponent'`
- `plugin='SubscriberComponent'`

两个组件的 `package` 都是 `'ros2lings_36_launch_composition'`。
