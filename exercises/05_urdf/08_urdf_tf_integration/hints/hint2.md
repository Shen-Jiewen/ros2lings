# 提示 2

部分答案提示：

- 问题 1: `robot_state_publisher` 使用一个 ROS 参数来接收 URDF 内容，
  这个参数的名字是"robot"加上"description"
- 问题 2: 固定关节只需要发布一次，所以用的是"静态"的 TF 话题
- 问题 3: 可动关节的**状态**由另一个节点发布，名字中带有"joint_state"
- 问题 4: 关节状态话题的名字就是发布内容的直接描述
- 问题 5: 每个 link 对应一个 TF 帧，所以帧数等于...
